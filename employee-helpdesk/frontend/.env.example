# Frontend .env example
# Copy to .env.local and fill in values (Vite uses .env.local)

# Backend URL — only needed for production deployment
# For local dev the Vite proxy handles /api → localhost:5000
# VITE_API_URL=https://your-deployed-backend.com
VITE_API_URL=
